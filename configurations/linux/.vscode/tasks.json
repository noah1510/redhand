{
    "tasks": [
        {
            "type": "shell",
            "label": "g++-7 build active file",
            "command": "${workspaceFolder}/scripts/build.sh",
            "args": [
                "-j",
                "8"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ],
    "version": "2.0.0"
}